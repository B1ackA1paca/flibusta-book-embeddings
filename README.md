# Flibusta Book Semantic Embeddings Training Pipeline

Репозиторий содержит модульный пайплайн для семантического анализа и получения эмбеддингов книг большого объема из датасета Флибусты. 

В основе решения лежит идея представления книги как последовательности смысловых чанков с последующей агрегацией их векторов через трансформер-суммаризатор.

## Архитектура и Стек
* **Основная модель:** `Alibaba-NLP/gte-multilingual-base` (замороженные веса + файнтюнинг через LoRA/PEFT).
* **Агрегатор (Summarizer):** Кастомный Transformer Encoder для сжатия последовательности векторов чанков в один итоговый эмбеддинг книги (позже будет переход на несколько эмбеддингов, отвечающих за разные аспекты книги).
* **Лосс-функция:** InfoNCE Loss (контрастивное обучение) для сближения векторов книг с их семантическими паспортами.
* **Оценка качества:** Валидация по метрике Recall@5.
* **Стек:** Python, PyTorch (с использованием смешанной точности AMP/Scaler), Hugging Face Transformers, PEFT, Scikit-Learn, Pandas, NumPy.

## Структура проекта
* `src/data_prep.py` — предобработка fb2-файлов, очистка текста, парсинг индекса `.inpx`.
* `src/models.py` — архитектура `BookSummarizer` и `FullBookEncoder`.
* `src/dataset.py` — пайплайн ленивой загрузки предобработанных векторов.
* `llm_generate.py` — генерация текстовых описаний с использованием vllm (параллельный батчинг и многое другое).
* `precompute.py` — скрипт для предварительного расчета эмбеддингов паспортов книг.
* `train.py` — цикл контрастивного обучения модели с валидацией.
* `inference.py` — итоговое использование обученной модели для создания векторной базы данных.
* `chech_db.py` — файл проверки базы данных.
* `vector_prepare.py` — превращение sql базы данных в LanceDB базу.

## Текущий статус (Roadmap)
- [x] Реализована модульная архитектура моделей и датасета.
- [x] Написан скрипт предварительного расчета эмбеддингов.
- [x] Реализован цикл контрастивного обучения (InfoNCE) с автоматическим ранним остановом (early stopping).
- [x] Добавить в пайплайн модуль автоматической генерации семантических паспортов через LLM.
- [x] Векторная база данных
- [ ] Reranker на LLM
- [ ] Смена предсказания одного вектора на набор из нескольких семантических представлений.

В начале было около 800к книг, после очистки(удаление дубликатов и обрезков книг) осталось порядка 300к, что все еще невозможно обработать на LLM в моих условиях. Прогон 200 книг занял 4 дня на rtx3070, поэтому обучение маленькой дистиляционной модели казалось единственным решением. 
Полученный скор: 0.7 recall@5 
Основная проблема: модель, которую я использую для генерации тескстовых описаний сама по себе слишком глупая(9B) и не может выполнять необходимую задачу извлечения информации.
